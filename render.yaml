services:
  - type: web
    name: agente-ebanistas
    runtime: node
    plan: free
    buildCommand: echo "listo"
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PASSWORD
        sync: false
      - key: OPENAI_API_KEY
        sync: false
